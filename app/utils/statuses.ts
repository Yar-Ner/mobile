export enum OrderStatuses {
  queued = 'queued',
  process = 'process',
  done = 'done',
  failed = 'failed',
}

export enum AddressStatuses {
  start = 'start',
  middle = 'middle',
  finish = 'finish',
  return = 'return',
}

export enum TaskStatuses {
  draft = 'draft',
  queued = 'queued',
  process = 'process',
  done = 'done',
}
